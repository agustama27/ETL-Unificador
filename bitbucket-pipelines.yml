image: python:3.12

pipelines:
  default:
    - step:
        name: Tests (Linux, Python 3.12)
        caches:
          - pip
        script:
          - pip install -e ".[test,etl,api]"
          - pytest
